stages:
  - deploy

deploy_frontend:
  image: node:latest
  stage: deploy
  only:
    - master
  before_script:
    - npm install -g vercel
  script:
    - vercel --token $VERCEL_TOKEN --prod --yes --name hackqc24
